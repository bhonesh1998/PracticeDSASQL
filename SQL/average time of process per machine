# Write your MySQL query statement below

select machine_id,round((et-st) * 2,3)
as processing_time from (
select
machine_id,
avg(case when activity_type='start' then timestamp else 0 end ) as st,
avg(case when activity_type='end' then timestamp else 0 end ) as et
from
Activity
group by machine_id) tt

